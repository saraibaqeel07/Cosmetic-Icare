export interface state {
  name: string;
  abbreviation: string;
}
export enum roles {
	GM = 'GM',
	superAdmin = 'admin',
	callCenter = 'call center',
	driver = 'driver',
	teamLead = 'team lead' // not being used currently.

}
